# Free key from https://console.groq.com. Optional: without it the app still works, using keyword rules only.
GROQ_API_KEY=
# Optional. Default is the fastest free model.
GROQ_MODEL=openai/gpt-oss-20b
# Optional. If set, visitors must enter this code or bring their own Groq key (protects a public deployment).
ACCESS_CODE=
